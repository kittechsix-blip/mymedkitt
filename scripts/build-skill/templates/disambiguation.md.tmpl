# Key Safety Disambiguations — resolve before routing on a "negative" or "benign" finding

These are the cross-consult traps where a result that looks reassuring is dangerous in
the wrong context. Each is the **canonical teaching from the myMedKitt source** plus the
gate to resolve before disposition. Generated — do not hand-edit; edit the source node or
`src/data/skill-gates.ts` and rebuild.

The unifying rule: **a "rule-out" or "benign" interpretation is valid only inside its
validated context (timing, pretest probability, exam). Establish that context first.**

{{disambiguations_block}}
