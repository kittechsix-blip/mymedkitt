# Cross-consult interactions — coordinate these together

The coordinator's job includes catching when two problems interact. When a case touches
both consults in a pair below, load both references and reason across them. This is the
content that makes a coordinator more than a router. Generated from
`src/data/skill-gates.ts` (CROSS_REFERENCES) — edit there and rebuild.

{{cross_references_block}}

> This list is seeded and grows consult-by-consult. If you spot an interaction not listed
> here, coordinate it from first principles and flag it for addition to the source.
